import React from "react";
import { Gesture, GestureDetector } from 'react-native-gesture-handler';
import { TouchableOpacity, StyleSheet, View } from "react-native";


function Ball({styles,msg}) {
  return (
      <View style={[styles]} />  );
}
export default Ball;